from setuptools import setup, find_packages

VERSION = "0.1"

setup(
    name="validatr",
    version=VERSION,
    author="Jake Dahn",
    description="validatr",
    packages=find_packages(),
    include_package_data=True,
)
